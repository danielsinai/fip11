# Get all configurations for the clsuters with an http request 
# Get all the leader from each partition in the cluster (cluster metdata request)
# Get all the quotas for each topic in the cluster

if __name__ == "__main__":
    clusters = 